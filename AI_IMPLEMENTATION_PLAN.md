# 技術文件：AI 輔助開發標準化實施方案 (Based on Karpathy Skills)

## 1. 背景與目的
在現代軟體開發流程中，AI 助手（如 Claude, Cursor, GitHub Copilot）已成為核心工具。然而，AI 常因缺乏工程背景而產生過度工程、邏輯碎片化或破壞現有代碼結構的問題。
本方案旨在透過導入「Andrej Karpathy 四大原則」，建立一套標準化的 AI 協作流程，以提升代碼質量並降低長期維護成本。

## 2. 核心實施策略

### 第一階段：基礎環境配置 (Standardized Environment)
我們將在所有開發專案中部署標準化的規則文件。
- **Action**: 
    1. 在所有新舊專案的根目錄強制加入 `CLAUDE.md`（或在 Cursor 中使用 `.cursorrules`）。
    2. 配置 `.gitignore` 確保這些本地開發輔助文件不會影響生產環境，除非團隊決定將其納入 CI。

### 第二階段：溝通協議定義 (Communication Protocol)
規範與 AI 對話的方式，將「指令式」轉化為「聲明式」。
- **實施規則**:
    - **禁止**：使用「幫我修好這段代碼」等模糊指令。
    - **必須**：提供「成功準則（Success Criteria）」。
    - **示例**：「請修正 X 功能的連線超時問題。成功準則：1. 新增一個測試用例模擬 5 秒延遲；2. 修正代碼使該測試通過；3. 確保不改動 `utils.py` 以外的任何檔案。」

### 第三階段：手術式變更審查 (Surgical Change Review)
針對 Git Diff 進行嚴格審查，確保 AI 的變更路徑是最小化的。
- **實施規則**:
    - AI 在提交代碼變更前，必須解釋為何需要改動這些行。
    - 任何「順便進行」的格式調整或重構必須被拆分成獨立的任務（Task），不得混合在功能開發中。

## 3. 工作流程整合 (Workflow Integration)

| 階段 | 步驟 | AI 行為約束 |
| :--- | :--- | :--- |
| **需求階段** | 指令輸入 | AI 必須回傳「假設列表」與「三種實作方案權衡」。 |
| **開發階段** | 代碼撰寫 | 遵循「簡潔優先」，禁止導入未要求的第三方庫或複雜設計模式。 |
| **驗證階段** | 自動化測試 | AI 必須先撰寫/更新測試，驗證通過後才視為 Task 完成。 |
| **收尾階段** | 代碼清理 | AI 自動移除因本次修改產生的「孤兒代碼（Unused code）」。 |

## 4. 預期效益與衡量指標
1. **降低 Code Bloat**：代碼行數預期減少 15-20%（透過避免過度抽象）。
2. **提升 Review 效率**：Git Diff 變動行數更集中，人工審核時間縮短 30%。
3. **穩定性提升**：強制執行「測試先行」邏輯，減少因 AI 盲目修改導致的 Regression Bug。

## 5. 結論
本方案不只是技術工具的導入，更是一種開發文化的轉變。透過將 AI 視為一名「需要清晰規格並遵守工程紀律」的資深開發者，我們可以最大化 AI 的產能，同時保持系統架構的純粹與穩定。
