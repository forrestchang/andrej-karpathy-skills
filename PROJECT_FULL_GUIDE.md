# Andrej Karpathy AI 編碼技能專案：深度解析與實施指南

> 本文件基於 [andrej-karpathy-skills](https://github.com/B0644144/andrej-karpathy-skills) 專案內容，旨在提供一套完整的技術介紹與工作環境實施方案。

## 問題所在 (The Problems)

根據 Andrej Karpathy 的觀察，當前大型語言模型（LLM）在編碼時存在以下核心痛點：

> 「模型會代你做錯誤假設，然後不假思索地執行。它們不管理自身的困惑，不尋求澄清，不呈現矛盾，不展示權衡，在應該提出異議時也不反駁。」

> 「它們真的很喜歡把代碼和 API 搞複雜，堆砌抽象概念，不清理死代碼……明明 100 行能搞定的事情，非要實現成 1000 行的臃腫架構。」

> 「它們有時仍會改動或刪除自己理解不足的代碼和註解，即使這些內容與任務本身無關。」

## 解決方案 (The Solution)

透過四個核心原則，將上述問題轉化為可控的工程標準：

| 原則 | 解決什麼問題 | 技術核心 |
|-----------|-----------|-----------|
| **編碼前思考** | 錯誤假設、隱藏困惑 | 顯式假設驗證 |
| **簡潔優先** | 過度複雜、臃腫抽象 | YAGNI (不需要就別寫) |
| **精準修改** | 無關編輯、破壞代碼風格 | 手術式變更 (Surgical Edit) |
| **目標驅動執行** | 執行偏差、缺乏驗證 | 測試優先與循環驗證 |

## 四個原則詳解 (Deep Dive)

### 1. 編碼前思考 (Think Before Coding)

**不要假設。不要隱藏困惑。呈現權衡。**

- **行為規範**：AI 必須在實施前列出其假設。若存在多種解釋，必須先詢問使用者而非盲目選擇。
- **職場應用**：在指派複雜任務（如「優化效能」）時，要求 AI 回傳「三種實作方案及其代價（Tradeoffs）」。

### 2. 簡潔優先 (Simplicity First)

**用最少的代碼解決問題。不要過度推測。**

- **行為規範**：禁止為「未來可能用到」的功能寫代碼。如果一個簡單的函數能解決，就不應該使用複雜的設計模式。
- **技術對照**：避免 AI 在單次折扣計算中引入「策略模式（Strategy Pattern）」等過度抽象。

### 3. 精準修改 (Surgical Changes)

**只碰必須碰的。只清理自己造成的混亂。**

- **行為規範**：嚴格匹配現有代碼風格（括號、引號、命名）。除非被要求，否則不准「順便」進行重構或格式美化。
- **技術對照**：修復 Bug 時，Git Diff 應僅顯示邏輯變動，不應出現大面積的縮進或格式調整。

### 4. 目標驅動執行 (Goal-Driven Execution)

**定義成功標準。循環驗證直到達成。**

- **行為規範**：將「指令」轉化為「可驗證的測試」。
- **職場應用**：要求 AI 遵循「撰寫失敗測試 -> 修正代碼 -> 驗證通過」的流程。

## 專案檔案架構解析

本專案透過多個配置文件實現對 AI 的自動化約束：

- **CLAUDE.md**: 核心憲章，定義 AI 的行為元邏輯。
- **EXAMPLES.md**: 正確與錯誤範例的對照表，是 AI 學習的最佳教材。
- **.cursor/rules/*.mdc**: Cursor 編輯器的原生規則，實現「全自動掛載」約束。
- **skills/**: 模組化的技能集，方便在不同平台間遷移工程標準。

## 工作環境實施方案 (Implementation Plan)

### 第一階段：環境標準化
- 在所有 Git 專案根目錄部署 `CLAUDE.md`。
- 配置 Cursor 或 Claude Code 自動讀取規則文件。

### 第二階段：溝通協議定義
- 規範 Prompt 格式：必須包含「背景、任務、成功準則」。
- 強制 AI 執行「思考步驟」：在輸出代碼前，先輸出 `Thought Process`。

### 第三階段：審核與迴圈
- 建立 AI 代碼審查標準：凡是不符合「精準修改」原則的 PR 一律退回。
- 定期更新 `EXAMPLES.md`，收錄團隊內部發現的 AI 典型錯誤。

## 如何判斷它在起作用

如果你看到以下情況，說明這套標準正在發揮作用：
- **Diff 極度乾淨**：只出現請求的變動。
- **代碼更易讀**：不再出現不明所以的抽象層。
- **溝通前置**：AI 在犯錯前會先問你「我的理解正確嗎？」。

## 權衡說明 (Tradeoff Note)

這套指南傾向於**謹慎而非速度**。對於瑣碎的任務（簡單拼寫修復），可以適度放寬；但對於核心邏輯變動，必須嚴格執行。

## 許可 (License)

MIT
